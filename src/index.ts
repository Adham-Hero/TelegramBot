import { connectDatabase } from './database/connection'; import { createBot } from './bot'; import { logger } from './utils/logger'; async function main() { await connectDatabase(); const bot = await createBot(); await bot.launch(); logger.info('Bot launched in polling mode'); process.once('SIGINT', () => bot.stop('SIGINT')); process.once('SIGTERM', () => bot.stop('SIGTERM')); } main().catch((err) => { logger.error('Fatal startup error', { err: err instanceof Error ? err.message : String(err), stack: err instanceof Error ? err.stack : undefined, }); process.exit(1); });